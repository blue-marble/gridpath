language: python
python:
  - "3.8"
before_install:
  - "sudo apt-get install coinor-cbc=2.10.5"
install:
  - pip install .
script:
  - python -m unittest discover tests

# whitelist
branches:
  only:
    - main
    - develop
    - travis

